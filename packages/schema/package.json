{
  "name": "@freemarket/schema",
  "version": "0.1.0",
  "description": "Shared Swarm JSON schema (TS types + validation) for FreeMarket clients.",
  "license": "MIT",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
